# Python
__pycache__/
*.pyc
# OS
.DS_Store
# secrets / local env (never commit keys)
.envrc
.env
*.local
# caches created at runtime
.cache/

# build artifacts — published via GitHub Releases, not committed
releases/*.zip
releases/*.mcpb

# editor/tool debris
*.bak-*

# paper full texts must never land in a public repo
skills/**/*.txt
!skills/**/references/*.txt
